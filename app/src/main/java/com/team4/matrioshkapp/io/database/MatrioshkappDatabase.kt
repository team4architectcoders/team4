package com.team4.matrioshkapp.base.io.database

import androidx.room.RoomDatabase

// @Database(entities = [MyEntitiy::class], version = 1, exportSchema = false)
abstract class MatrioshkappDatabase : RoomDatabase()
