apply plugin: 'jacoco'

jacoco {
    toolVersion = "0.8.3"
}

task jacocoDataTestReport(type: JacocoReport, dependsOn: ['test']) {

    reports {
        xml.enabled = true
        html.enabled = true
    }

    def excludes = [
            '**/R.class',
            '**/R$*.class',
            '**/*$ViewInjector*.*',
            '**/BuildConfig.*',
            '**/Manifest*.*',
            '**/*Test*.*',
            'android/**/*.*',
            '**/*Fragment.*',
            '**/*Activity.*'
    ]
    def mainSrc = "${project.projectDir}/src/main/java"

    sourceDirectories = files([mainSrc])
    classDirectories = fileTree(dir: "${buildDir}/classes/kotlin", excludes: excludes)
    executionData = files("$buildDir/jacoco/test.exec")
}
